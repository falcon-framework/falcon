---
"@falcon-framework/sdk": patch
---

SDK prereleases are published from long-lived branches instead of pull-request previews: **`release/beta`** uses Changesets prerelease (`@beta` on npm), and **`dev`** publishes alpha snapshots (`x.y.z-alpha.<sha>`, `@alpha`). See the README (SDK publishing → Prerelease branches).
