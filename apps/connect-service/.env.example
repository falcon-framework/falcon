# The monorepo loads the repo root `.env` via `packages/infra/alchemy.run.ts`.
# Use `/Users/benn/Documents/Projects/dzwei/falcon/.env.example` as the single source
# of truth for shared Auth / Connect worker bindings, including:
# - DATABASE_URL
# - BETTER_AUTH_SECRET
# - BETTER_AUTH_URL
# - CORS_ORIGIN
# - CONNECT_JWT_PRIVATE_KEY
# - CONNECT_JWT_PUBLIC_KEY
# - CONNECT_ACCESS_TOKEN_TTL_SECONDS
#
# No additional connect-service-only env vars are currently required here.
